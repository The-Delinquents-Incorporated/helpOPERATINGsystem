Fix, v0.4.0.5:

Knowledge Base) The search function is not visible, and when finding it -- it  doesnt display text.

AI Solver) Add all markdown formatting rendering for the AI's response when the "Show Work" toggle is enabled.

Knowledge Base, formatting) fix the formatting of any sub-scripts, superscripts and charges for all polyatomic ions, reaction types, and compounds via LaTeX formatting. --> So H2O is actually H_2O and is shown the way in class. Must change in the knowledge base itself the in the front-end with LaTeX formatting and rendering.